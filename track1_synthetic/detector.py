"""Track 1｜合成影像偵測（B 暫時放的佔位版本，CONVENTIONS §4.10）。

A 完成真正的模型後，這個檔案會被整個換掉，呼叫端（api/routes.py）
不需要跟著改一行——這是階段0敲定介面契約的回報。

B 先放這份佔位版本的原因：A 的進度目前不確定，但 B 需要先把 /verify
端點串通、驗證整條後端管線走得通，不能等 A。
"""


def detect_synthetic(face_images: list) -> dict:
    """佔位版本，永遠回傳固定值。A 完成後取代此檔案。

    2026-08-21：fakeProbability 暫時從 0.87 改成 0.05（低於
    config.SYNTHETIC_THRESHOLD=0.50，等於這層固定判定為「通過」）。
    原本的 0.87 會讓 Track 1 每次都貢獻 0.20（權重）×87 ≈ 17.4 分風險，
    不管其他四層真人測試結果如何，都會拖著整體分數往上——這在正常測試
    Track 2/3/4 真人校準時是雜訊，不是有意義的訊號（反正這層本來就
    不是真的在判斷，只是固定值）。改低純粹是為了讓真人測試時能看到
    其他四層真正的表現，不代表 Track 1 本身有任何進展。**這整個檔案
    等 A 交付真模型後會被整個換掉，這個暫時調整到時候會自然消失，
    不用特別記得回改。**
    """
    return {
        "fakeProbability": 0.05,
        "topSignals": [
            {"label": "臉部邊界混合痕跡", "weight": 0.38},
            {"label": "高頻紋理不一致", "weight": 0.31},
            {"label": "跨影格閃爍", "weight": 0.25},
        ],
    }
