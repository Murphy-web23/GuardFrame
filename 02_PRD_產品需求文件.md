# PRD｜產品需求文件
## GuardFrame：即時生成式換臉攻擊防禦驗證引擎

---

## 一、產品定位與使用者

**定位**：GuardFrame 是一套嵌入銀行數位開戶流程的驗證引擎，透過五層防禦機制（動作挑戰、合成影像偵測、生理訊號、照明響應、遮擋一致性），特別針對即時生成式換臉攻擊進行防禦，並提供銀行風控人員完整的驗證證據與後台管理介面。

**呈現方式**：雙視角展示——前台模擬銀行開戶系統的使用者流程（敘事工具），後台為真正交付的核心產品（風控管理介面）。

**使用者角色**

| 角色 | 使用情境 | 對應介面 |
|---|---|---|
| 開戶申請者 | 完成銀行的開戶驗證流程 | 前台 |
| 銀行風控／審核人員 | 查看驗證紀錄、處理人工複核案件、調閱單筆完整證據 | 後台 |
| 銀行系統（非人類使用者） | 呼叫驗證引擎 API，接收風險分數與建議結果，自行決定帳戶開通與否 | API 層 |

---

## 二、核心功能列表

### 前台：開戶流程

流程順序：①基本資料 → ②簡訊驗證＋15 分鐘倒數開始 → ③身分證明 → ④人臉驗證 → ⑤帳戶設定 → ⑥結果

| 功能 | 說明 |
|---|---|
| F1 | 六步驟開戶流程 |
| F2 | 基本資料表單，含「填入示範資料」按鈕（展示用） |
| F3 | 簡訊驗證碼，通過後啟動 15 分鐘全域倒數計時，涵蓋範圍包含後續證件上傳與人臉驗證兩步驟 |
| F4 | 證件上傳與四角自動矯正（YOLO-pose 角點偵測為主，古典 Canny＋透視變換為自動 fallback） |
| F5 | 人臉驗證：動作挑戰（4動作固定時長）＋照明挑戰＋遮擋挑戰，全程約 23 秒。進入本步驟時，若全域倒數剩餘時間 < 23 秒，先提示使用者剩餘時間過短，建議重新開始；一般情況下不主動提示，直接進行驗證 |
| F6 | 倒數歸零時：中斷當前流程，捨棄本輪已產生之驗證資料，退回簡訊驗證步驟重新開始 |
| F6.5 | 帳戶設定：僅 `decision.verdict === "pass"` 時進入。提供帳戶類型選擇（第一類／第三類，不提供第二類——法規要求第二類須為臨櫃驗證之舊戶，本系統全線上開戶不適用）、交易密碼設定、通知偏好、條款同意，送出後 `accountResult` 由 `pending_setup` 更新為 `opened`。**本功能編號為規格補件，原始 PRD 定稿時遺漏，於後續 v0 前端交接階段補齊** |
| F7 | 結果頁三態呈現：開戶成功／待審核／驗證未通過（不揭露拒絕原因） |

### 驗證引擎：五層防禦

| 功能 | 說明 |
|---|---|
| F8 | 對照組：隨機順序動作挑戰判定（眨眼／左轉／右轉／揮手） |
| F9 | Track 1：合成影像偵測（視覺基礎模型特徵＋線性分類頭，現行主線為 SigLIP2，另有 DINOv2 挑戰線比較中）。訓練資料區分三類：真人未修改、真人底片經換臉或表情操控、人物影像全由 AI 生成 |
| F10 | Track 2：生理訊號偵測（rPPG，三區域一致性檢驗） |
| F11 | Track 3：照明響應分析（螢幕隨機色序＋互相關＋立體幾何） |
| F12 | **Track 4：遮擋一致性分析**（身分嵌入連續性，多次揮手循環偵測）。專門針對其餘四層皆可能被即時換臉攻擊突破的情境設計，是本系統的核心防禦層 |
| F13 | 五層加權融合，產出 0-100 風險分數 |
| F14 | 三段式決策（通過／人工複核／拒絕） |

### 各層防禦的能力邊界

五層並非各自都能涵蓋全部攻擊，而是刻意設計為失效條件互不重疊。完整情境對應見 SRS §一之二，此處摘要三個關鍵事實：

| 事實 | 說明 |
|---|---|
| Track 1 無法判斷「是否為當下拍攝」 | Track 1 僅分析畫面內容有無生成或操縱痕跡。對於臉部與背景皆真實、僅擷取路徑為預錄注入的攻擊，Track 1 預期無法偵測，須由對照組與 Track 3 負責 |
| 即時換臉可通過對照組與 Track 3 | 攻擊者本人在現場操作，動作能正確完成、光照反應真實，僅 Track 4 之遮擋一致性可偵測此類攻擊 |
| 全 AI 生成人物為五層皆可偵測之情境 | 此類攻擊在畫面內容、生理訊號、光學響應上皆有異常，是唯一五層可交叉驗證的情境 |

### 後台：風控管理介面

| 功能 | 說明 |
|---|---|
| F15 | 密碼登入（法遵稽核用途） |
| F16 | 驗證紀錄列表，可依狀態篩選（通過／人工複核／拒絕） |
| F17 | 單筆詳情頁：五層對照、rPPG 波形、照明雙線圖、遮擋身分穩定度曲線（含多段遮擋循環標示） |
| F18 | 「對照組漏判」標記（凸顯被傳統活體偵測誤判為真人、但被系統攔下的案件） |
| F19 | VLM 輔助審核模組。僅於人工複核案件觸發，針對 Track 4 標出的異常影格逐格分析並生成描述性摘要，協助風控人員快速定位可疑之處，縮短單筆案件的審核時間。VLM 不參與判定，僅提供觀察描述，最終複核決策仍由人員做出。**本功能為產品完整版本之核心功能，於四週開發期程中排序在後，優先確保五層防禦主體完成後再行實作** |

### 技術選型理由：為何不直接採用單一 VLM 判定

以目前的多模態大模型（VLM）能力，理論上可以用一個夠強的模型直接
對整段驗證影片下「真人／攻擊」的二元判斷，取代五層獨立訊號的架構。
團隊評估過這個做法，最終選擇現行的五層拆分式設計，理由如下：

| 考量面向 | 說明 |
|---|---|
| **法規與隱私合規** | 使用者的即時人臉影片屬於高敏感個資與生物特徵資料。若呼叫第三方雲端 VLM API，等同將生物特徵資料傳輸至銀行控管範圍以外的環境處理，不符合金融業個資與資安規範對生物特徵資料的處理要求。這是設計上被明確排除的路徑，不是能力落差 |
| **自架大模型的硬體門檻** | 若為避開雲端隱私疑慮改採銀行自架大型 VLM，需要 GPU 叢集等級的運算基礎設施，且每一次驗證請求都需要占用可觀算力。拆成五個輕量模組，是為了讓驗證能在使用者端一般裝置（筆電內建鏡頭、家用網路）上即時完成，不依賴銀行端為每一次驗證負擔昂貴算力 |
| **可解釋性與可稽核性** | 單一 VLM 輸出的「真人／攻擊」判斷是黑箱決策，風控與法遵單位難以向監理機關說明具體判斷依據。五層獨立訊號（心跳偵測、光反應相關係數、遮擋前後身分連續性等）各自輸出可量化的中間結果與理由，符合金融業對決策留痕、可稽核的要求，也是後台 F17 單筆詳情頁能夠呈現五層對照數據的基礎 |
| **縱深防禦（Defense in Depth）** | 單一模型是單一失效點，攻擊者只需找到一種方法騙過該模型即可突破全部防線。五條原理互不相依的獨立訊號（生理訊號、物理光反射、跨影格身分嵌入、行為挑戰反應）要求攻擊者同時突破五種不同偵測機制，尤其 Track 4（遮擋一致性）是專門針對「其餘四層皆可能被即時換臉突破」情境設計的核心防禦層，這種可組合式的縱深防禦是單一模型架構無法提供的 |
| **VLM 的定位：輔助人工複核，而非取代主判定** | 團隊並非完全排除 VLM——F19 已規劃將 VLM 用於人工複核階段，針對 Track 4 標出的異常影格生成描述性摘要，協助風控人員快速定位可疑之處。這是刻意將 VLM 限定在「輔助人類判斷」的角色，不讓其直接輸出最終決策，呼應前述可解釋性與合規的考量 |

**簡言之**：團隊具備直接串接 VLM 的能力與知識，但基於金融場景的合規
要求、終端裝置的硬體限制、決策可解釋性、以及縱深防禦的資安設計原則，
主動選擇了五層獨立訊號拆分的架構。這是根據場景限制做出的工程判斷，
而非技術能力的邊界。

---

## 三、呈現方式與技術架構總覽

### 呈現形式

| 項目 | 內容 |
|---|---|
| 產品形式 | Web App（前台開戶流程＋後台風控介面），供銀行部署於自有網域或整合進既有開戶系統 |
| 對外介接方式 | RESTful API，銀行核心系統呼叫驗證引擎取得風險分數與建議結果 |
| 展示形式 | 本專題以完整可操作的 Demo 呈現，前台模擬銀行開戶流程、後台呈現真實風控查閱情境 |

### 使用者旅程圖

```
① 基本資料填寫
        ↓
② 簡訊驗證碼（通過後啟動 15 分鐘全域倒數）
        ↓
③ 證件上傳與自動矯正
        ↓
④ 人臉驗證（動作挑戰＋照明挑戰＋遮擋挑戰，約 23 秒）
        │
        │  ← 此步驟產出的影片與資料，即為驗證引擎的輸入
        ↓
   ┌────┴────┐
   │ 進入驗證引擎（見系統架構總覽圖）│
   └────┬────┘
        ↓
   風險分數與建議結果回傳
        ↓
   ┌────┼────┐
   ↓         ↓         ↓
 通過      人工複核     拒絕
   ↓         ↓         ↓
⑤ 帳戶設定  待審核提示   ⑥ 驗證未通過
   ↓
⑥ 開戶完成

（若 15 分鐘倒數歸零，任一階段中斷，退回步驟②重新開始）
```

### 系統架構總覽圖

```
使用者端（瀏覽器攝影機＋螢幕）
   │
   │  錄影檔（約23秒）＋ 挑戰指定資料 ＋ 光序列時間戳
   ↓
API 層（接收驗證請求）
   │
   ↓ 影像品質前置檢查（不合格→退回要求重錄）
   ↓ 影像前處理（抽影格、人臉偵測、對齊）
   │
   ├──────────┬──────────┬──────────┬──────────┐
   ↓          ↓          ↓          ↓          ↓
 對照組     Track1      Track2     Track3     Track4
 動作挑戰    合成偵測    生理訊號    照明響應    遮擋一致性
   │          │          │          │          │
   └──────────┴──────────┴──────────┴──────────┘
                        ↓
              五層加權融合 → 0-100 風險分數
                        ↓
        ┌───────────────┼───────────────┐
        ↓               ↓               ↓
      通過           人工複核           拒絕
        │               │               │
        │        VLM 輔助審核模組         │
        │      （異常影格摘要）           │
        └───────────────┼───────────────┘
                        ↓
              寫入資料庫（完整證據留存）
                        ↓
        ┌───────────────┴───────────────┐
        ↓                               ↓
   回傳銀行系統                    後台風控介面
  （建議結果＋風險分數）           （供人工查閱與稽核）
```

### 非目標（Out of Scope）

- 不含證件真偽辨識與 OCR 欄位抽取
- 不含裝置層 capture-source 驗證（虛擬攝影機偵測、SDK hook 防禦等）
- 不含金融聯徵中心等外部資料查詢介接
- 不含帳戶開通後的持續監控機制（定期核對身分等）
- 不取代銀行對開戶結果的最終法律責任
